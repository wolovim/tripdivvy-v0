import React, {
  Component,
  StyleSheet,
  Text,
  View
} from 'react-native';

class Dues extends Component {
  render() {
    return (
      <View style={styles.container}>
        <Text>Dues Screen: Coming Soon!</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: '#F5FCFF',
    justifyContent: 'center',
  },
});

export default Dues;
